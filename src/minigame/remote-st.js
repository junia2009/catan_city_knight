// 散策部屋で配る「見た目の状態」。
//
// サーバー(server/walk-relay.js)とクライアント(remote.js / walk-mode.js)の
// 両方が使うので、どちらからも読める src/ 側に1つだけ置く
// ── 2か所に書くと、片方だけ増やしたときに静かにずれる。
//
// 姿勢を全部送るのは重いので、「どの姿勢を使うか」だけを数字で送り、
// 細かい角度は受け取った側が自分で作る。

export const ST = {
  walk: 0,   // 立っている・歩いている
  air: 1,    // 跳んでいる
  fall: 2,   // 海へ落ちている・沈んでいる
  fish: 3,   // 釣っている
};

export const ST_MAX = 3;
